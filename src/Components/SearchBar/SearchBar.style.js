import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container : {
        padding : 10,
        margin : 5,
        borderRadius : 5,
        borderWidth : 2,
        borderColor : '#e0e0e0'

    },
    text_input : {
        fontSize : 15,
        fontWeight : 'bold'
    }
})